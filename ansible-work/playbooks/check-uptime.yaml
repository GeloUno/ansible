---
- name: Check uptime
  hosts: all
  gather_facts: false
  become: false

  tasks:
    - name: Run uptime
      command: uptime
      register: uptime_result
      changed_when: false
      failed_when: false

    - name: Show uptime
      debug:
        msg: "{{ inventory_hostname }}: {{ uptime_result.stdout }}"
